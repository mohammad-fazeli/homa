# کارهای باقی‌مانده

اولویت از بالا به پایین. موردهای تیک‌خورده در دور قبلی انجام شده‌اند.

## اولویت بالا

- [ ] مهاجرت تاریخ جلسات قدیمی از فرمت محلی سیستم به ISO-8601 تا تقویم و ثبت حضور RFID برای دادهٔ قبلی هم کار کند
- [ ] انتخاب پورت RFID از داخل برنامه (لیست COMها + ذخیرهٔ انتخاب)، نه فقط `RFID_PORT` یا اولین پورت
- [ ] پشتیبانی واقعی از چند دوره برای هر مشتری در UI (الان فقط آخرین دوره دیده و ویرایش می‌شود)
- [ ] باندل کردن فونت وزیر داخل برنامه تا آفلاین هم درست نمایش داده شود
- [ ] پشتیبان‌گیری و بازیابی دیتابیس (خروجی/ورودی فایل SQLite از منوی برنامه)

## داده و منطق کسب‌وکار

- [ ] جلوگیری از تداخل دو جلسه در یک ساعت برای مشتری‌های مختلف (الان تقویم نشان می‌دهد ولی ذخیره را سخت نمی‌گیرد مگر روی سلول اشغال‌شده کلیک شود)
- [ ] ثبت حضور خارج از تلرانس ۲۰ دقیقه با تأیید دستی اپراتور
- [ ] کم/زیاد کردن جلسه از لیست مشتریان همراه با انتخاب تاریخ در تقویم
- [ ] اعتبارسنجی تلفن و کد ملی (طول، رقم، تکراری بودن با پیام واضح در فرم)
- [ ] صفحه یا مودال مشاهدهٔ مشتری جدا از ویرایش
- [ ] استفاده یا حذف ستون بلااستفادهٔ `Users.average`
- [ ] یکسان‌سازی نوع‌ها: `shared/types.ts` و `electron/db/types.ts` الان دو نسخهٔ جدا هستند

## رابط کاربری

- [ ] اصلاح بخش «عیب‌یابی» در `README.md` (عنوان این بخش هنگام نوشتن مستندات حذف شده)
- [ ] نمایش نام برنامه در نوار عنوان سفارشی
- [ ] خالی بودن تقویم وقتی جلسه‌ای نیست (متن راهنما)
- [ ] شکستن باندل سنگین renderer (هشدار Vite حدود ۷۱۰ کیلوبایت)
- [ ] یکدست کردن مسیرها: `/user/new` در برابر `/users/...`
- [ ] اصلاح املای IPC `get-calender` به `get-calendar` با سازگاری نسخهٔ قبل

## کیفیت و انتشار

- [ ] تست خودکار برای مدل‌های SQLite (ساخت کاربر، یکتایی، ثبت جلسه، RFID use-session)
- [ ] آیکون و فایل‌های `build/` برای electron-builder
- [ ] تنظیمات NSIS (میانبر دسکتاپ، مسیر نصب، نام یکتای اپ)
- [ ] اسکریپت lint (`tsc` + eslint) در CI یا `npm test`
- [ ] بررسی آسیب‌پذیری‌های npm (`npm audit`) بدون شکستن native modules
- [ ] اجرای دستی `npm run dist` روی ویندوز و تست نصب‌کننده

## انجام‌شده (مرجع)

- [x] مستندات `README.md`
- [x] اصلاح باگ به‌روزرسانی کاربر (`uidCart` / `id`)
- [x] اتصال `deleteUser` به preload و UI حذف
- [x] تشخیص dev با `app.isPackaged` و بستن DevTools در نسخهٔ نهایی
- [x] تاریخ جدید جلسات به ISO-8601
- [x] اسکن RFID در فرم مشتری جلسه را مصرف نمی‌کند
- [x] لاگ مالی هنگام ثبت/تغییر هزینه
- [x] جستجوی سمت دیتابیس
- [x] مسیر ویرایش `/users/edit/:id`
- [x] `postinstall` برای ماژول‌های native
