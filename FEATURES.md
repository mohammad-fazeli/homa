# قابلیت‌های هما برای آموزشگاه چندکلاسه

این سند مرجع محصول است. مخاطب: آموزشگاهی با چند کلاس همزمان، چند مربی، دوره‌های فردی و گروهی، حضور RFID و صندوق واقعی.

وضعیت هر مورد: **انجام‌شده** یعنی در همین نسخه پیاده شده، **بعدی** یعنی طراحی شده و آماده اسپرینت بعد.

---

## ۱. مدل ذهنی آموزشگاه

هما دیگر یک میز تک‌کلاسه نیست. واحدهای اصلی:

| موجودیت | نقش |
| --- | --- |
| کلاس (اتاق) | فضای فیزیکی با رنگ، ظرفیت و یادداشت |
| مربی | فرد مسئول جلسه؛ در یک ساعت فقط یک کلاس |
| بسته دوره | الگوی آماده (مثلاً ۸ جلسه / ۱۲ جلسه با قیمت) |
| دوره مشتری | ثبت‌نام یک نفر روی یک بسته، با کلاس و مربی پیش‌فرض |
| جلسه | یک نوبت در یک ساعت؛ چند دانش‌آموز تا سقف ظرفیت همان کلاس |
| پرداخت | پول واقعی دریافت‌شده، جدا از مبلغ قرارداد دوره |
| وضعیت جلسه | رزرو، حاضر، غایب، لغو، جبرانی |

**کلاس گروهی** بدون جدول جدا ساخته می‌شود: چند مشتری در همان کلاس، همان ساعت. تداخل سراسری ساعت حذف شده؛ تداخل فقط داخل یک کلاس (ظرفیت) و برای یک مربی (یک جا در هر ساعت) است.

---

## ۲. کلاس‌ها و مربیان — انجام‌شده

### کلاس
- نام، رنگ، ظرفیت (تعداد نفر در هر ساعت)، یادداشت
- سه کلاس پیش‌فرض بعد از ارتقا: کلاس ۱ / ۲ / ۳ با ظرفیت ۸، ۸ و ۱۲
- حذف کلاس، ارجاع دوره‌ها و جلسات را خالی می‌کند (دادهٔ مشتری پاک نمی‌شود)
- رنگ کلاس روی تقویم، داشبورد و پروفایل دیده می‌شود

### مربی
- نام، نام خانوادگی، تلفن اختیاری، رنگ، یادداشت
- در یک ساعت نمی‌تواند دو کلاس موازی داشته باشد
- فیلتر تقویم بر اساس مربی

### بسته دوره
- نام، تعداد جلسه، هزینه پیشنهادی، مدت جلسه (دقیقه)
- دو بسته پیش‌فرض: ۸ جلسه و ۱۲ جلسه
- در فرم مشتری با یک کلیک هزینه و تعداد جلسه پر می‌شود

صفحهٔ **آموزشگاه** (`/academy`) مدیریت کلاس، مربی، بسته، **گروه نام‌دار** و **تعطیلات** را نشان می‌دهد.

### گروه نام‌دار
- یک گروه مثل «پیانو کودکان» با کلاس، مربی، بسته، روزهای هفته و ساعت
- افزودن مشتری‌های موجود به‌عنوان عضو؛ برای هر نفر دورهٔ همان گروه ساخته می‌شود
- اگر ظرفیت کلاس پر باشد عضو جدید رد می‌شود
- **تولید جلسات برای همه**: همان ساعت برای تمام اعضا، تا سقف تعداد جلسه
- حذف گروه، دوره‌ها و جلسات مشتری را پاک نمی‌کند
- مربی در یک ساعت فقط اگر کلاس دیگری (اتاق دیگر) داشته باشد تداخل دارد؛ چند دانش‌آموز در همان کلاس مجاز است

---

## ۳. دوره و زمان‌بندی — انجام‌شده

- هر دوره **عنوان** دارد (نه فقط «دوره ۱»)
- کلاس، مربی، بسته، تاریخ انقضا و یادداشت روی دوره
- تولید جلسات تکرارشونده: انتخاب روزهای هفته + ساعت + پر کردن تا سقف تعداد جلسه
- رزرو جدید، کلاس و مربی را از دوره به ارث می‌برد مگر خلافش مشخص شود
- چند دانش‌آموز در یک خانهٔ ساعت تا سقف ظرفیت کلاس
- رزرو جدید روی روز تعطیل آموزشگاه رد می‌شود
- اگر ظرفیت پر باشد ذخیره با پیام واضح رد می‌شود
- اگر مربی در آن ساعت جای دیگری باشد ذخیره رد می‌شود
- تاریخ انقضا: فیلتر «منقضی» در فهرست مشتریان برای دوره‌های باقی‌ماندهٔ تاریخ‌گذشته

---

## ۴. وضعیت جلسه — انجام‌شده

| وضعیت | اعتبار دوره | جای کلاس | معنی عملیاتی |
| --- | --- | --- | --- |
| رزرو | مصرف نمی‌شود | اشغال می‌کند | منتظر حضور |
| حاضر | مصرف می‌شود | اشغال می‌کند | RFID یا ثبت دستی |
| غایب | مصرف می‌شود | اشغال می‌کند | نیامد ولی جلسه سوخت |
| لغو | مصرف نمی‌شود | آزاد می‌شود | نوبت برای دیگری باز است |
| جبرانی | مصرف می‌شود | اشغال می‌کند | جلسهٔ اضافهٔ برگزارشده |

RFID و «ثبت حضور» وضعیت را **حاضر** می‌کنند. لغو حضور به **رزرو** برمی‌گردد. از تقویم، پروفایل و داشبورد می‌توان غایب یا لغو هم زد.

---

## ۵. تقویم چندکلاسه — انجام‌شده

- خانهٔ ساعت چند دانش‌آموز را به‌صورت چیپ رنگی (رنگ کلاس) نشان می‌دهد
- اگر بیشتر از ظرفیت نمایش باشد، «+N» دیده می‌شود
- کلیک روی خانه، فهرست همهٔ افراد آن نوبت را باز می‌کند (پروفایل، حاضر، غایب، لغو)
- فیلتر سریع: همهٔ کلاس‌ها / هر کلاس / همهٔ مربیان / هر مربی
- خط «الان» و برجسته‌سازی امروز حفظ شده است
- چاپ برنامهٔ امروز از داشبورد (CSS چاپ، بدون سایدبار)

---

## ۶. مالی واقعی — انجام‌شده

صندوق از قرارداد جداست. بدهی از اسناد مالی محاسبه می‌شود، نه از قیمت دوره به‌تنهایی.

- انواع سند: دریافت، استرداد، تخفیف
- روش‌ها: نقد، کارتخوان، کارت‌به‌کارت، چک، آنلاین + شماره پیگیری
- بدهی = max(0، قرارداد − (دریافت + تخفیف − استرداد))
- بستانکاری اگر بیش از قرارداد پرداخت شود
- صندوق امروز / هفته / ماه، نرخ وصول و نرخ تسویه
- عمر بدهی (تا ۳۰ / ۶۰ / ۹۰ / بیش از ۹۰ روز)
- فهرست بدهکاران با دریافت یک‌کلیکی و جزئیات دوره
- دفتر اسناد با فیلتر روش، نوع، مشتری و بازه
- گزارش دوره‌ای روزانه + چاپ صندوق
- ثبت و ویرایش سند از صفحهٔ مالی و پروفایل مشتری
- خروجی CSV اسناد و بدهکاران
- دوره‌های قدیمی هنگام ارتقا یک دریافت معادل هزینه می‌گیرند
- هنگام ساخت دوره، گزینهٔ «هزینه الان پرداخت شد»

---

## ۷. داشبورد عملیاتی — انجام‌شده

- کارت‌های قبلی + **اشغال کلاس‌های امروز** (رزرو / ظرفیت / حاضر)
- برنامهٔ امروز با نام کلاس و مربی
- اعتبار رو به اتمام + دوره‌های نزدیک به انقضا
- خروجی CSV برنامهٔ امروز با کلاس
- میانبر کیوسک

---

## ۸. مشتری — انجام‌شده

- یادداشت آزاد روی پرونده
- فیلترها: همه، جلسه امروز، اعتبار کم، بدون کارت، منقضی، بدهکار
- در لیست: نام دوره، کلاس، بدهی
- پروفایل: دوره‌ها با کلاس/مربی/انقضا، وضعیت هر جلسه، ثبت پرداخت، بدهی

---

## ۹. کیوسک حضور — انجام‌شده

مسیر `/kiosk` (میانبر `Ctrl+Shift+K`):
- تمام‌صفحه بدون سایدبار
- ساعت بزرگ و وضعیت RFID
- بازخورد واضح بعد از کارت (همان Overlay)
- فهرست نوبت‌های نزدیک، گروه‌بندی‌شده بر اساس کلاس
- مناسب میز ورودی؛ اپراتور لازم نیست داخل پرونده‌ها باشد

---

## ۱۰. قفل PIN و صدا — انجام‌شده

- PIN اختیاری در تنظیمات؛ هش در `settings.json` ذخیره می‌شود، خود PIN به UI برنمی‌گردد
- اگر PIN فعال باشد، بعد از باز شدن برنامه صفحهٔ قفل می‌آید
- بوق کوتاه هنگام حضور موفق / کارت نامعتبر

---

## ۱۱. تجربهٔ کاربری (الزام طراحی)

- فارسی و RTL؛ تقویم شنبه تا جمعه
- رنگ کلاس پایدار و خوانا روی چیپ‌ها
- خانه‌های شلوغ تقویم باید چند نفر را نشان بدهند، نه فقط نفر اول
- پیام خطا مشخص باشد: ظرفیت کلاس، تداخل مربی، کارت نامعتبر
- کیوسک باید از فاصلهٔ یک متری خوانا باشد (ساعت و نام بزرگ)
- صفحهٔ آموزشگاه باید ساخت کلاس جدید را در کمتر از ۱۵ ثانیه ممکن کند

---

## ۱۲. یادآوری پیامک و واتساپ — انجام‌شده

بدون سرور ابری. برنامه فهرست را می‌سازد و اپراتور از همین رایانه پیام را باز می‌کند.

- صفحهٔ **یادآوری‌ها** (`/reminders`): جلسهٔ فردا، اعتبار کم (۲ جلسه یا کمتر)، بدهی
- واتساپ (`wa.me`)، پیامک سیستم، یا کپی متن
- متن پیام‌ها قابل ویرایش است؛ متغیرهایی مثل نام، ساعت، کلاس، مانده و بدهی
- ارسال امروز برای هر مشتری ثبت می‌شود تا دوباره در فهرست نیاید
- میانبر از داشبورد و تنظیمات (نام آموزشگاه)

---

## ۱۳. وارد کردن Excel مشتریان — انجام‌شده

لیست قدیمی را از فایل `.xlsx` / `.xls` / `.csv` می‌آورد؛ بدون اینترنت و بدون سرور.

- از صفحهٔ **مشتریان** یا تنظیمات یا Ctrl+K: **ورود Excel**
- پیش‌نمایش: آماده، تکراری (تلفن / کد ملی / کارت)، خطا
- قالب نمونه قابل دانلود است (نام، تلفن، کد ملی، دوره، هزینه، جلسات، باقی‌مانده، پرداخت)
- اگر پرداخت خالی باشد بدهی می‌ماند؛ پرداخت فقط وقتی ثبت می‌شود که ستونش پر باشد
- ستون باقی‌مانده اعتبار دوره در نرم‌افزار است؛ تاریخ جلسه از اکسل ساخته نمی‌شود
- شمارهٔ بدون صفر اول (مثل خروجی اکسل) و کد ملی بدون صفر اول اصلاح می‌شود

---

## ۱۴. پشتیبان خودکار روزانه — انجام‌شده

بدون ابر. یک پوشه روی همین رایانه انتخاب می‌شود.

- تنظیمات → پایگاه داده: انتخاب پوشه، روشن/خاموش، نگهداری ۷ / ۱۴ / ۳۰ روز
- با باز شدن برنامه و هر ساعت، فایل `homa-auto-YYYY-MM-DD.sqlite` همان روز نوشته می‌شود
- پشتیبان دستی (`homa-backup-...`) در همان پوشه پاک نمی‌شود
- اگر پوشه نباشد، خطا در تنظیمات دیده می‌شود و برنامه قفل نمی‌شود
- بازیابی همان فایل‌های sqlite را می‌پذیرد
- پوشهٔ عکس‌ها کنار همان فایل (`…-photos`) کپی می‌شود

---

## ۱۵. عکس مشتری و مربی — انجام‌شده

عکس روی همین رایانه کنار پایگاه داده ذخیره می‌شود؛ بدون ابر.

- انتخاب از فرم مشتری، پروفایل، فهرست، داشبورد و کارت مربی
- JPEG کوچک‌شده (حداکثر ضلع ۵۱۲) در `photos/`
- اگر عکس نباشد همان حروف اول نام نمایش داده می‌شود
- هنگام حضور RFID اگر عکس باشد روی پیام خوش‌آمد دیده می‌شود
- پشتیبان روزانه و دستی پوشهٔ عکس را هم برمی‌دارد

---

## ۱۶. تعطیلات آموزشگاه — انجام‌شده

روزهای بسته در تب **تعطیلات** صفحهٔ آموزشگاه تنظیم می‌شوند.

- تعطیل موردی با نام (مثلاً نوروز) روی یک تاریخ محلی
- تعطیلی هفتگی با انتخاب روز هفته (شنبه تا جمعه)
- رزرو جدید، جابه‌جایی جلسه و تولید جلسات گروه روی روز تعطیل رد می‌شود
- تولید جلسات تکراری روز تعطیل را رد می‌کند و به‌جای آن روز باز بعدی را پر می‌کند
- جلسات از قبل رزروشده پاک نمی‌شوند؛ ثبت حضور و غایب روی آن‌ها همچنان ممکن است
- ستون روز تعطیل در تقویم هفتگی متمایز است و کلیک خالی رزرو نمی‌سازد

---

## ۱۷. فاز بعد (هنوز پیاده نشده)

این‌ها عمداً برای اسپرینت‌های بعدی مانده‌اند تا مدل فعلی پایدار بماند:

1. **چند اپراتور و لاگ حسابرسی** (چه کسی حضور دستی زد)
2. **اتاق انتظار / لیست انتظار** وقتی ظرفیت پر است
3. **گزارش اشغال ساعتی، نرخ غیبت، ارزش طول عمر مشتری**
4. **فاکتور PDF و ارسال فایل**
5. **تمدید یک‌کلیکی** با کپی الگوی هفتهٔ قبل
6. **تم تیره**

---

## ۱۸. قوانین کسب‌وکار (باید همیشه برقرار بماند)

1. تلفن و کد ملی یکتا؛ کارت RFID یکتا
2. ظرفیت کلاس در هر ساعت سقف همزمانی است
3. مربی در هر ساعت حداکثر یک نوبت دارد
4. جلسهٔ لغوشده نه اعتبار را می‌سوزاند نه ظرفیت را اشغال می‌کند
5. حاضر و غایب هر دو از اعتبار کم می‌کنند
6. درآمد صندوق فقط از جدول پرداخت است؛ هزینهٔ دوره قرارداد است
7. RFID فقط وقتی صفحهٔ ساخت/ویرایش مشتری باز نیست جلسه مصرف می‌کند
8. همهٔ داده‌ها محلی است؛ بدون سرور ابری
9. رزرو جدید روی روز تعطیل آموزشگاه مجاز نیست؛ جلسات قبلی را پاک نمی‌کند

---

## ۱۹. مسیرهای جدید UI

| مسیر | صفحه |
| --- | --- |
| `/academy` | کلاس‌ها، مربیان، بسته‌ها، گروه‌ها، تعطیلات |
| `/reminders` | یادآوری واتساپ و پیامک |
| `/kiosk` | کیوسک حضور |
| `/` … `/settings` | همان مسیرهای قبلی با دادهٔ غنی‌شده |
